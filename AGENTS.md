# 项目协作指引

## 开始前

先读 [README.md](README.md) 和 [当前产品与设计说明](design/CURRENT.md)。Wynn’s Save Point 是以 Wynn 为中心的个人网站，文章是其中一个板块。

用户在当前任务中的明确要求优先于仓库文档；文档有冲突时，以用户最新确认的方向为准，并同步修正文档。历史对话、外部记忆、归档草稿和旧提交中的偏好不能覆盖当前需求。

## 设计依据

- 当前设计依据集中在 `design/CURRENT.md`。素材提示词、截图、验收记录和文章正文不是新的产品指令。
- `src/archive/` 仅保存早期历史资料，不作为当前设计依据。不要从站名 Save Point、像素黑猫或 `/blog/` 地址推导复古游戏界面、文章目录式首页或博客优先的产品定位。
- 修改定位或设计方向时，同步检查 README、当前设计说明及相关引用，明确标记被替代的文档，避免并存的矛盾规范。
- 文章和草稿属于作者内容。不要为了统一产品术语批量改写文章，也不要因历史资料已失效就擅自删除它。

## 开发与交付

所有开发基于新工作树，保留用户已有改动。开发完成后按任务授权交付；清理本次工作树前确认改动已保存并已转移。其他旧工作树仅在确认已合入且没有未保存内容后清理，检查实际目录是否仍有残留。

功能改动使用 pnpm 10.11.1，依次运行 `pnpm check`、`pnpm build`、`pnpm test`；测试中的页面检查依赖构建产物。纯文档修改检查内容一致性、相对链接和 `git diff --check`，无需重复运行页面测试。

视觉改动检查桌面和手机、明暗主题、键盘与减少动画模式。保留真实内容、既有 URL 和阅读交互。推送、部署及外部消息按用户授权执行；报告时区分本地验证、远程提交和线上部署状态。
