# 夏日桌面验收

验收时间：2026-09-09 至 2026-09-10。环境：Windows、Codex 内置 Chromium 浏览器、pnpm 10.11.1。

## 自动检查

- `pnpm install --frozen-lockfile` 成功，原有依赖版本保留。
- `pnpm check`：0 errors / 0 warnings / 0 hints。
- `pnpm build`：11 个页面构建成功，Pagefind 索引 4 个真实内容页面。
- `pnpm test`：13 项通过，覆盖系统主题与持久化、筛选、真实链接和锚点、播放器状态/重试/竞态、页面资源释放。
- `git diff --check` 通过。
- 项目、笔记集合本来为空，构建会提示空集合；页面展示真实空状态。

## 浏览器检查

- 1440 × 900 桌面、390 × 844 手机、320 × 740 小屏完成检查。320px 下文档 scrollWidth 与 clientWidth 均为 305px（另有15px滚动条），Dock 图标完整落在容器内。
- 首页晴天/夏夜、手机纵向布局、日期时间、黑猫头像、真实最新文章及五个入口正常。与 `summer-reference.png` 对照，保留草图布局和玻璃层级；实际头像、文章和空歌单使用真实状态。
- 文章、项目、笔记、关于、搜索与404均可访问，页面切换后主题保持。
- 标签筛选 Tauri 得到1条记录；搜索“模块”有真实结果，不存在的词显示无结果。离开搜索后再次进入只初始化1个搜索框，无控制台错误。
- 长文章标题在手机正常换行；正文、代码和表格不撑宽页面。代码复制显示“已复制”；代码块与复制按钮数量一致。
- 目录点击后当前章节高亮、阅读进度更新。Giscus 评论 iframe 正常加载；没有发送评论或回应。
- 测试音频主动播放后：首页→列表→文章→后退→前进仍播放，音频数量始终为1。最后版本再次验证首页→列表→首页，时间0→2.89→5.97秒连续增长，回到首页仍在原播放器位置。
- 切歌、展开/收起、暂停、进度跳转通过；支持 Range 的测试音频可定位到119.9秒。首次音频503错误出现重试提示，再次点击恢复。刷新后暂停，不自动播放。

## 边界

正式歌单保持空配置，合成测试音频仅由本地测试服务提供，不写入发布资源。
减少动画和减少透明度已有CSS降级规则；未在真实 Safari/iOS 设备上运行此次验收。
本次交付仅在本地，未推送远程或触发线上部署。

## 截图

`qa/home-day.png`、`qa/home-night.png`、`qa/mobile-day.png`、`qa/mobile-night.png`、`qa/article-desktop.png`、`qa/article-mobile.png`。
